# .env.example

# URL de conexión a la base de datos MongoDB para pruebas e2e
MONGO_URL=mongodb://localhost:27017/histora-test

# Puerto en el que corre la aplicación (opcional si usas NestJS con ConfigModule)
PORT=3000

# Clave secreta para JWT (si aplica)
JWT_SECRET=tu_clave_secreta

# Otros valores que uses en tu entorno de pruebas
# API_KEY=tu_api_key
# NODE_ENV=test